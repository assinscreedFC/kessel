import { Module } from "@nestjs/common";

// Placeholder V1Module — rempli par les plans 02/03 (routes /api/v1/*).
// Ne pas ajouter de controllers/providers ici : chaque plan downstream importe et remplit ce module.
@Module({})
export class V1Module {}
