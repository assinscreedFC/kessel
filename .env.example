# Kessel — variables d'environnement (exemple). NE JAMAIS commiter de vrai secret (CLAUDE.md security).
# Copier en `.env` (non commité) et adapter.

# Postgres — MÊME datastore pour Prisma (métier) et Better Auth (organization/member/role, Plan 04).
# Un seul espace d'id organisation : pas de divergence Prisma <-> Better Auth.
DATABASE_URL="postgresql://kessel:kessel@localhost:5432/kessel?schema=public"
